# Syndicator reads API keys from the environment.
# This file only documents the variables; do not commit a filled-in .env.

# Required for translation, captions and vision crop focus:
OPENAI_API_KEY=sk-...

# Optional, only used by the future agent mode (Cursor SDK):
CURSOR_API_KEY=cursor_...
